class Hall {
  constructor(id, fullName, dept, image, capacity) {
    this.id = id;
    this.fullName = fullName;
    this.department = dept;
    this.image = image;
    this.capacity = capacity;
  };
}

export default Hall;